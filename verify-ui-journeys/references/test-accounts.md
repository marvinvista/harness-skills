# Test Accounts

Keep only non-secret local setup notes here.

Suggested entries:
- `basic-user`: clean account for onboarding and empty states
- `power-user`: populated account with representative data
- `admin-user`: role-based access checks

Store secrets in the repo's approved secret mechanism, not in this file.
