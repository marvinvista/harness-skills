# UI Verification Checklist

Check:
- the target path loads
- the expected element or state appears
- loading and error states still behave
- adjacent navigation paths still work
- before-and-after evidence exists for the reported bug when practical

For fixes with user-visible impact, summarize the outcome in product language, not only DOM language.
