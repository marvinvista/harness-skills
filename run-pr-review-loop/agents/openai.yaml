interface:
  display_name: "Run PR Review Loop"
  short_description: "Drive a PR through iterative review"
  default_prompt: "Use $run-pr-review-loop to self-review this branch, gather feedback, and iterate until the PR is ready."
