# Common Regression Patterns

Look again at:
- renamed files that silently dropped imports
- config changes without matching docs or deploy notes
- schema changes without backfill or rollback story
- UI fixes that solved one state but broke another
- new helpers that duplicate an existing abstraction
