# Stack Layout

Prefer one isolated directory per worktree for:
- env files
- process IDs
- logs
- screenshots or test artifacts

Keep the stack discoverable from the worktree itself rather than a shared scratch directory when practical.
