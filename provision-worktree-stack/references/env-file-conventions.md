# Env File Conventions

Keep env files:
- generated from defaults plus explicit overrides
- local to the worktree
- uncommitted unless they are intentional templates

Use clear names such as:
- `.env.worktree`
- `.env.local.worktree`
- `.codex/worktree.env`
